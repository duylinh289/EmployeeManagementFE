import { CustomDateTimeFormatPipe } from './custom-date-time-format.pipe';

describe('CustomDateTimeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomDateTimeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
